import 'package:flutter/material.dart';

class IncomingPage extends StatelessWidget {
  const IncomingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Incoming Page'));
  }
}
